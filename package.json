{
  "name": "q-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "www",
    "src"
  ],
  "scripts": {
    "dev": "bun run --cwd www dev",
    "backend": "bun run --cwd src dev"
  }
}
